const WORK_TYPE_ENUM = {
  ONSITE: "onsite",     // Kantor
  REMOTE: "remote",     // Jarak jauh
  HYBRID: "hybrid",     // Gabungan
};

const WORK_TYPE_VALUES = Object.values(WORK_TYPE_ENUM);

module.exports = {
  WORK_TYPE_ENUM,
  WORK_TYPE_VALUES,
};
